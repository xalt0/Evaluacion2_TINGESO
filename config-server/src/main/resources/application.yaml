server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/xalt0/Evaluacion2_TINGESO
          search-paths: config-data
  application:
    name: config-server